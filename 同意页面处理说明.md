# 同意页面处理说明

## 问题

当账号首次使用 HMCL 登录时，会遇到同意页面（Consent Page），需要点击"接受"按钮授权应用访问 Xbox Live 信息。

同意页面 URL 示例：
```
https://account.live.com/Consent/Update?mkt=ZH-CN&uiflavor=web&id=290794&...
```

## 为什么 HTTP 脚本无法自动点击？

1. **传统登录页面**：使用标准 HTML 表单，可以直接提取表单数据并 POST 提交
2. **同意页面**：使用 React 单页应用（SPA），按钮点击触发 JavaScript 函数，通过 AJAX/Fetch API 发送请求

HTTP requests 库无法执行 JavaScript，因此无法模拟点击行为。

## 解决方案

### 方案 1：使用 Selenium 自动化（推荐）

使用 `auto_login_selenium.py` 脚本，它会启动真实浏览器并自动点击"接受"按钮。

#### 安装依赖

```bash
pip install selenium
```

#### 安装 ChromeDriver

macOS:
```bash
brew install chromedriver
```

或者从官网下载：https://chromedriver.chromium.org/

#### 使用方法

```bash
python auto_login_selenium.py
```

选项：
- 显示浏览器：输入 `y` 可以看到浏览器操作过程
- 无头模式：输入 `N` 在后台运行

### 方案 2：手动授权一次

1. 在浏览器中手动登录一次 HMCL
2. 点击"接受"按钮完成授权
3. 之后 Cookie 会记住授权状态，无需再次同意
4. 以后使用 `auto_login_http.py` 即可

## 技术细节

同意页面的 HTML 结构：

```html
<button type="submit" 
        data-testid="appConsentPrimaryButton" 
        class="ext-primary ext-button ...">
    接受
</button>
```

这个按钮在 React 应用中，点击后会：
1. 调用 JavaScript 事件处理函数
2. 提取 ServerData 中的认证令牌
3. 通过 Fetch API 发送 POST 请求到某个 API 端点
4. 重定向到返回 URL

ServerData 包含的关键字段：
- `sAuthenticationToken`: 认证令牌
- `apiCanary`: CSRF 保护令牌
- `sUnauthSessionID`: 会话 ID
- `sClientId`: 客户端 ID（HMCL: 000000004C794E0A）

## 对比

| 特性 | auto_login_http.py | auto_login_selenium.py |
|------|-------------------|----------------------|
| 速度 | 快（2-3秒） | 慢（10-15秒） |
| 依赖 | requests | selenium + ChromeDriver |
| 同意页面 | ✗ 不支持 | ✓ 支持 |
| 资源占用 | 低 | 高 |
| 适用场景 | 已授权账号 | 首次授权 |

## 建议

1. 首次使用账号：用 `auto_login_selenium.py` 完成授权
2. 后续使用：用 `auto_login_http.py` 快速登录
3. Web 系统集成：首次提示用户手动授权，之后自动登录
